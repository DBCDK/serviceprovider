'use strict';

import {assert} from 'chai'; // eslint-disable-line
import * as OpenHoldingStatus from '../client.js'; // eslint-disable-line

describe('Test OpenHoldingStatus getHolding', () => {


  it('Dummy', function(done) {

    done();

    /*
    this.timeout(5000);
    setTimeout(done, 5000);
    const config = {
      wsdl: 'http://openholdingstatus.addi.dk/2.2/?wsdl'
    };

    OpenHoldingStatus.init(config);
    let result = OpenHoldingStatus.getHolding({
      responderId: '710100',
      pid: '870970-basis:43512449'
    });

    result.then(function (holdingsResult) {
      console.log(holdingsResult);
      assert.equal(holdingsResult['responder']['willLend'], 'true');
      done();
    });
    */

  });
});
