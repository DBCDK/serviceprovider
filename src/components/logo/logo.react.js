'use strict';
let React = require('react');

let FrontPage = React.createClass({
  render: () => {
    return (
      <div>
        <img src='/logo.png' />
      </div>
    );
  }
});

module.exports = FrontPage;
