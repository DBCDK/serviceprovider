'use strict';

import {assert} from 'chai';
// import React from 'react';
// import TestUtils from 'react/lib/ReactTestUtils';
// import Login from '../Login.component';

describe('Test the ResetPassword component', () => {
  it('should work', () => {
    assert.equal(true, true);
  });
});
