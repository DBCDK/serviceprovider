extends layout

block prepend footerscripts
   if pagescript
      script(src="/js/" + pagescript + '?v=1.0.2' defer=true)


block content
   #page.row !{content}
   script window.PAGE_DATA = !{data};
