definitions:
  RecommendationResponse:
    type: array
    items:
      - $ref: "#/definitions/Recommendation"
  Recommendation:
    type: object
    properties:
      creator:
        type: array
        items:
          type: string
      pid:
        type: array
        items:
          type: string
      title:
        type: array
        items:
          type: string
      val:
        type: number
